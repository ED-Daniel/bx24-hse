# ✅ Чеклист для запуска интеграции с Bitrix24

## Перед началом работы

### 1. Проверка окружения

```bash
# Активировать виртуальное окружение
source .venv/bin/activate

# Проверить установленные зависимости
pip list | grep -E "fastapi|pydantic|httpx|email-validator"
```

Ожидаемый результат:
```
fastapi              0.115.6
pydantic             2.9.2
httpx                0.28.1
email-validator      2.3.0
```

### 2. Проверка конфигурации

```bash
# Проверить наличие .env файла
ls -la .env

# Проверить BITRIX24_WEBHOOK_URL
grep BITRIX24_WEBHOOK_URL .env
```

Ожидаемый формат:
```
BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/1/webhook_key/
```

### 3. Проверка field_mapping.json

```bash
# Проверить наличие файла
cat field_mapping.json
```

Должен содержать:
- `universal_lists.polls.iblock_id: 17`
- `universal_lists.educational_programs.iblock_id: 18`
- `bitrix24_entities.deals.custom_fields.UF_CRM_1755626160`
- `bitrix24_entities.deals.custom_fields.UF_CRM_1755626174`

---

## Тестирование

### Шаг 1: Unit тесты

```bash
source .venv/bin/activate
python test_integration_endpoints.py
```

✅ Ожидаемый результат: **5/5 тестов прошли**

Проверяет:
- ✅ Импорты модулей
- ✅ Регистрацию роутера в FastAPI
- ✅ Структуру endpoints
- ✅ Pydantic схемы

### Шаг 2: Запуск сервера

```bash
source .venv/bin/activate
python main.py
```

✅ Ожидаемый вывод:
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 3: Проверка Health endpoint

В новом терминале:

```bash
curl http://localhost:8000/api/v1/integration/health
```

✅ Ожидаемый ответ:
```json
{
    "status": "healthy" или "degraded",
    "field_mapping_loaded": true,
    "constants_configured": true,
    "bitrix24_api_available": true/false,
    "service": "integration",
    "version": "1.0.0"
}
```

**Примечание:**
- `status: "degraded"` и `bitrix24_api_available: false` - нормально, если Bitrix24 временно недоступен
- `status: "healthy"` - все работает отлично

### Шаг 4: Swagger UI

Откройте в браузере:
```
http://localhost:8000/docs
```

✅ Должны быть видны endpoint'ы:
- `POST /api/v1/integration/postPoll`
- `POST /api/v1/integration/postAnswer`
- `GET /api/v1/integration/health`

---

## Первый реальный тест

### Тест 1: Регистрация опросной формы

```bash
curl -X POST "http://localhost:8000/api/v1/integration/postPoll" \
  -H "Content-Type: application/json" \
  -d '{
    "poll_id": 999999,
    "poll_name": "Тестовая опросная форма",
    "poll_language": "ru",
    "employee_email": "test@hse.ru"
  }'
```

✅ Ожидаемый ответ (если форма НЕ существует):
```json
{
    "status": "success",
    "message": "Связанная опросная форма для ID 999999 создана в CRM или уже существует",
    "description": "Опросная форма успешно зарегистрирована в системе",
    "poll_id": 999999,
    "is_successful": true
}
```

✅ Ожидаемый ответ (если форма УЖЕ существует):
```json
{
    "status": "success",
    "message": "Связанная опросная форма для ID 999999 уже существует в CRM",
    "poll_id": 999999,
    "is_successful": true
}
```

❌ Возможные ошибки:
```json
{
    "status": "error",
    "message": "Опросная форма с ID 999999 не создана",
    "description": "Failed to create poll form in Bitrix24",
    "poll_id": 999999,
    "is_successful": false
}
```

**Что проверить при ошибке:**
1. Доступен ли Bitrix24 API (проверьте /health)
2. Правильный ли BITRIX24_WEBHOOK_URL в .env
3. Есть ли права у вебхука на создание элементов списков

### Тест 2: Отправка ответа (БЕЗ образовательных программ)

```bash
curl -X POST "http://localhost:8000/api/v1/integration/postAnswer" \
  -H "Content-Type: application/json" \
  -d '{
    "header_data": {
      "poll_id": 999999,
      "answer_id": 1,
      "create_time": "2025-10-22T10:00:00.000Z",
      "form_kind": 2,
      "analytics": {
        "url": "https://example.com",
        "params": {
          "utm_source": "test",
          "utm_medium": "integration"
        },
        "cookies": {
          "roistat_visit": "12345"
        },
        "ip": "127.0.0.1",
        "date": "2025-10-22 13:00",
        "timeZone": "Europe/Moscow"
      }
    },
    "data": {
      "firstname": "Тест",
      "lastname": "Тестов",
      "email": "test_integration@example.com"
    }
  }'
```

✅ Ожидаемый ответ:
```json
{
    "status": "success",
    "message": "Успешно обработано. Создана 1 общая сделка (без указания ОП)",
    "description": "Контакт и сделка созданы/обновлены успешно",
    "poll_id": 999999,
    "answer_id": 1,
    "is_successful": true
}
```

### Тест 3: Проверка в Bitrix24

После успешного теста 2, зайдите в Bitrix24 и проверьте:

1. **Контакт создан:**
   - CRM → Контакты
   - Найдите: test_integration@example.com
   - Проверьте: Имя = "Тест", Фамилия = "Тестов"

2. **Сделка создана:**
   - CRM → Сделки
   - Найдите сделку с контактом test_integration@example.com
   - Проверьте поле "Комментарии" - должен быть JSON с cookies и analytics

---

## Тестирование с образовательными программами

⚠️ **ВАЖНО:** Для этого теста нужны РЕАЛЬНЫЕ образовательные программы в Bitrix24!

### Подготовка: Создание тестовой программы

Вариант 1 - Вручную в Bitrix24:
1. Зайдите в: CRM → Списки → Образовательные программы (или универсальный список с ID=18)
2. Создайте элемент: NAME = "Тестовая программа"

Вариант 2 - Используйте существующую программу:
- Найдите реальное название программы в списке
- Используйте ТОЧНОЕ название (с учетом регистра!)

### Тест 4: Отправка ответа С образовательной программой

```bash
curl -X POST "http://localhost:8000/api/v1/integration/postAnswer" \
  -H "Content-Type: application/json" \
  -d '{
    "header_data": {
      "poll_id": 999999,
      "answer_id": 2,
      "create_time": "2025-10-22T10:00:00.000Z",
      "form_kind": 2,
      "analytics": {
        "url": "https://example.com",
        "params": {"utm_source": "test"},
        "cookies": {"roistat_visit": "12345"},
        "ip": "127.0.0.1",
        "date": "2025-10-22 13:00",
        "timeZone": "Europe/Moscow"
      }
    },
    "data": {
      "firstname": "Иван",
      "lastname": "Петров",
      "email": "ivan.petrov@example.com",
      "educational_program_1": ["Тестовая программа"]
    }
  }'
```

✅ Ожидаемый ответ:
```json
{
    "status": "success",
    "message": "Успешно обработано. Создано сделок: 1 - Тестовая программа (NEW)",
    "poll_id": 999999,
    "answer_id": 2,
    "is_successful": true
}
```

❌ Ошибка (программа не найдена):
```json
{
    "status": "error",
    "message": "Не удалось сохранить ответ 2",
    "description": "Образовательные программы не найдены в системе: Тестовая программа",
    "poll_id": 999999,
    "answer_id": 2,
    "is_successful": false
}
```

**Решение:** Проверьте точное название программы в Bitrix24!

### Тест 5: Множественные программы

```bash
curl -X POST "http://localhost:8000/api/v1/integration/postAnswer" \
  -H "Content-Type: application/json" \
  -d '{
    "header_data": {
      "poll_id": 999999,
      "answer_id": 3,
      "create_time": "2025-10-22T10:00:00.000Z",
      "form_kind": 2,
      "analytics": {
        "url": "https://example.com",
        "params": {"utm_source": "test"},
        "cookies": {"roistat_visit": "12345"},
        "ip": "127.0.0.1",
        "date": "2025-10-22 13:00",
        "timeZone": "Europe/Moscow"
      }
    },
    "data": {
      "firstname": "Мария",
      "lastname": "Сидорова",
      "email": "maria.sidorova@example.com",
      "educational_program_1": ["Тестовая программа", "Тестовая программа"]
    }
  }'
```

✅ Ожидаемый ответ:
```json
{
    "status": "success",
    "message": "Успешно обработано. Создано сделок: 2 - Тестовая программа (NEW), Тестовая программа (EXISTING)",
    "poll_id": 999999,
    "answer_id": 3,
    "is_successful": true
}
```

---

## Интеграционные тесты (опционально)

⚠️ **ВНИМАНИЕ:** Эти тесты создают РЕАЛЬНЫЕ данные в Bitrix24!

```bash
source .venv/bin/activate
python test_integration_service.py
```

Тесты будут интерактивными - нужно подтверждать каждый шаг.

---

## Проверка логов

### Просмотр логов сервера

Логи выводятся в консоль при запуске `python main.py`.

Ожидаемый формат логов:
```
======================================================================
🚀 START PROCESSING WEBHOOK
   Poll ID: 999999
   Answer ID: 2
   Email: ivan.petrov@example.com
======================================================================

📋 STEP 1: Validating incoming data...
✅ Validation passed

🔍 STEP 2: Finding poll form...
✅ Poll form found

👤 STEP 3: Finding or creating contact...
✅ Contact ready

🎓 STEP 4: Processing 1 educational programs...
✅ Deal enriched successfully

======================================================================
✅ WEBHOOK PROCESSED SUCCESSFULLY
======================================================================
```

---

## Проверка в Bitrix24

### Контакты (CRM → Контакты)

Найдите тестовые контакты по email:
- test_integration@example.com
- ivan.petrov@example.com
- maria.sidorova@example.com

Проверьте поля:
- ✅ Имя, Фамилия заполнены
- ✅ Email корректный
- ✅ UTM метки заполнены (если были в запросе)

### Сделки (CRM → Сделки)

Найдите сделки по контактам.

Проверьте:
- ✅ Контакт привязан к сделке
- ✅ Образовательная программа указана (если была в запросе)
- ✅ Поле "Комментарии" содержит JSON с:
  - cookies
  - additional_fields
  - analytics
- ✅ UTM метки заполнены
- ✅ ID Roistat заполнен (если был roistat_visit в cookies)

### Опросные формы (CRM → Списки → Опросные формы)

Проверьте:
- ✅ Создан элемент с poll_id = 999999
- ✅ Название = "Тестовая опросная форма"

---

## Решение проблем

### Проблема 1: Bitrix24 API недоступен

```bash
# Проверьте health endpoint
curl http://localhost:8000/api/v1/integration/health
```

Если `bitrix24_api_available: false`:
1. Проверьте интернет-соединение
2. Проверьте BITRIX24_WEBHOOK_URL в .env
3. Проверьте права вебхука в Bitrix24
4. Попробуйте открыть URL вебхука в браузере

### Проблема 2: Опросная форма не найдена

```
Error: "Опросная форма с ID 999999 не найдена в системе"
```

Решение:
1. Сначала вызовите POST /postPoll для регистрации формы
2. Убедитесь, что poll_id одинаковый в обоих запросах

### Проблема 3: Образовательная программа не найдена

```
Error: "Образовательные программы не найдены в системе: Тестовая программа"
```

Решение:
1. Проверьте ТОЧНОЕ название программы в Bitrix24 (регистр важен!)
2. Создайте программу вручную в списке IBLOCK_ID=18
3. Используйте название БЕЗ лишних пробелов

### Проблема 4: Validation error

```
422 Unprocessable Entity
```

Решение:
1. Проверьте формат JSON запроса
2. Убедитесь, что обязательные поля присутствуют
3. Проверьте формат email (должен быть валидным)
4. Посмотрите детали ошибки в ответе API

---

## Финальная проверка

### Чеклист готовности к продакшену

- [ ] ✅ Все unit тесты проходят (5/5)
- [ ] ✅ Сервер запускается без ошибок
- [ ] ✅ Health endpoint возвращает status: "healthy"
- [ ] ✅ postPoll создает опросные формы в Bitrix24
- [ ] ✅ postAnswer создает контакты
- [ ] ✅ postAnswer создает сделки
- [ ] ✅ Множественные программы обрабатываются корректно
- [ ] ✅ Дополнительные поля сохраняются в JSON
- [ ] ✅ UTM метки записываются в сделки
- [ ] ✅ Cookies сохраняются в COMMENTS
- [ ] ✅ Логи детальные и понятные

### Дополнительные рекомендации для продакшена

1. **Настройте мониторинг:**
   - Prometheus + Grafana
   - Логирование в файл или систему логов
   - Алерты на критические ошибки

2. **Добавьте HTTPS:**
   - Nginx с SSL сертификатом
   - Let's Encrypt для бесплатных сертификатов

3. **Настройте автозапуск:**
   - Systemd service
   - Docker + Docker Compose
   - Kubernetes deployment

4. **Добавьте базу данных:**
   - PostgreSQL для хранения истории запросов
   - Redis для кеширования

5. **Реализуйте RBAC:**
   - Проверка прав на основе employee_email
   - JWT токены для аутентификации

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи** в консоли сервера
2. **Проверьте health endpoint**
3. **Запустите тесты** для диагностики
4. **Изучите документацию:**
   - INTEGRATION_API_GUIDE.md
   - PROCESS_WEBHOOK_GUIDE.md
   - IMPLEMENTATION_SUMMARY.md

---

**Интеграция готова к работе! 🚀**
